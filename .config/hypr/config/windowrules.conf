# ┏━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━┓
# ┃                    Windowrules Configuration                ┃
# ┗━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━┛

# Windows Rules https://wiki.hyprland.org/0.45.0/Configuring/Window-Rules/ #

# Prevent forced fullscreen
#windowrulev2 = suppressevent fullscreen, class:.*

# Float Necessary Windows
windowrule = match:class ^(org.pulseaudio.pavucontrol), float on
windowrule {
    name = floating_pip
    match:class = ^()$
    match:title = ^(Picture in picture)$

    float = on
}
windowrule = match:class ^()$, match:title ^(Save File)$, float on
windowrule = match:class ^()$, match:title ^(Open File)$, float on
windowrule = match:class ^(LibreWolf)$, match:title ^(Picture-in-Picture)$, float on
windowrule = match:class ^(blueman-manager)$, float on
windowrule = match:class ^(xdg-desktop-portal-gtk|xdg-desktop-portal-kde|xdg-desktop-portal-hyprland)(.*)$, float on
windowrule = match:class ^(polkit-gnome-authentication-agent-1|hyprpolkitagent|org.org.kde.polkit-kde-authentication-agent-1)(.*)$, float on
windowrule = match:class ^(CachyOSHello)$, float on
windowrule = match:class ^(zenity)$, float on
windowrule = match:class ^()$, match:title ^(Steam - Self Updater)$, float on
# Increase the opacity
# # windowrule = opacity 0.92, class:^(Thunar|nemo)$
# windowrule = opacity 0.96, class:^(discord|armcord|webcord)$
# windowrulev2 = opacity 0.9 0.7, class:^(telegram-desktop)$
# windowrule = opacity 0.95, title:^(NetEase Cloud Music Gtk4)$
# windowrulev2 = opacity 0.9 0.7, class:^(zen)$ # zen browser opacity
# windowrulev2 = opacity 0.9 0.7, class:^(Thunar)$ # thunar file manager opacity
# windowrulev2 = opacity 0.9 0.7, class:^(vesktop)$ # discord (vesktop) opacity
# windowrulev2 = opacity 0.9 0.7, class:^(yandex-music)$ # yandex-music opacity
# windowrulev2 = opacity 0.9 0.7, class:^(obsidian)$ # obsidian opacity
# windowrulev2 = opacity 1.0 0.7, class:^(Emacs)$ # emacs opacity
# windowrulev2 = opacity 0.9 0.7, class:^($terminal)$ # termilan opacity
# General window rules
windowrule = match:title ^(Picture-in-Picture)$, float on
windowrule = match:title ^(Picture-in-Picture)$, size 960 540
windowrule = match:title ^(Picture-in-Picture)$, move 25%-
windowrule = match:title ^(imv|mpv|danmufloat|termfloat|nemo|ncmpcpp)$, float on
windowrule = match:title ^(imv|mpv|danmufloat|termfloat|nemo|ncmpcpp)$, move 25%-
windowrule = match:title ^(imv|mpv|danmufloat|termfloat|nemo|ncmpcpp)$, size 960 540
windowrule = match:title ^(danmufloat)$, pin on
windowrule = match:title ^(danmufloat|termfloat)$, rounding 5
windowrule = match:class ^(kitty|Alacritty)$, animation slide right
windowrule = match:class ^(org.mozilla.firefox)$, no_blur on
# Decorations related to floating windows on workspaces 1 to 10
windowrule = border_size 2, match:float 1, match:workspace w[fv1-10]
windowrule = border_color $cachylblue, match:float 1, match:workspace w[fv1-10]
windowrule = rounding 8, match:float 1, match:workspace w[fv1-10]
# Decorations related to tiling windows on workspaces 1 to 10
windowrule = border_size 3, match:float 1, match:workspace f[1-10]
windowrule = rounding 4, match:float 1, match:workspace f[1-10]
# Windows Rules End #

# Workspaces Rules https://wiki.hyprland.org/0.45.0/Configuring/Workspace-Rules/ #
# workspace = 1, default:true, monitor:$priMon
# workspace = 6, default:true, monitor:$secMon
# Workspace selectors https://wiki.hyprland.org/0.45.0/Configuring/Workspace-Rules/#workspace-selectors
# workspace = r[1-5], monitor:$priMon
# workspace = r[6-10], monitor:$secMon
# workspace = special:scratchpad, on-created-empty:$applauncher
# no_gaps_when_only deprecated instead workspaces rules with selectors can do the same
# Smart gaps from 0.45.0 https://wiki.hyprland.org/0.45.0/Configuring/Workspace-Rules/#smart-gaps
workspace = w[tv1-10], gapsout:5, gapsin:3
workspace = f[1], gapsout:5, gapsin:3
# Workspaces Rules End #

# Layers Rules #
layerrule = animation slide top, match:namespace logout_dialog
# layerrule = animation popin 50%, waybar
layerrule = animation slide down, match:namespace waybar
layerrule = animation fade 50%, match:namespace wallpaper
# Layers Rules End #
