# Yomipv configuration
# Boolean options use yes/no
# Numeric values use numbers, decimals, or percentages
# Colors use hex, rgb, or rgba formats

# --- Anki settings ---

# AnkiConnect URL
ankiconnect_url=127.0.0.1:8765

# AnkiConnect API key
ankiconnect_api_key=

# Anki DB build settings
# Quote field names that contain spaces
ankidb_word_fields=word Word expression Expression
ankidb_reading_fields=reading Reading "Word Reading"

# Deck and Note type
deck=Mining
note_type=Lapis

# Note fields
expression_field=Expression
expression_furigana_field=ExpressionFurigana
reading_field=ExpressionReading
pitch_accents_field=
pitch_position_field=PitchPosition
pitch_categories_field=PitchCategories
sentence_field=Sentence
sentence_furigana_field=SentenceFurigana
secondary_sentence_field=
expression_audio_field=ExpressionAudio
sentence_audio_field=SentenceAudio
selection_text_field=SelectionText
definition_field=MainDefinition
glossary_field=Glossary
image_field=Picture
freq_sort_field=FreqSort
freq_field=Frequency
miscinfo_field=MiscInfo
dictionary_pref_field=

# Update card and append media if term exists
update_if_exists=yes
# Fields updated:
# sentence_field
# sentence_furigana_field
# secondary_sentence_field
# sentence_audio_field
# image_field
# miscinfo_field

# Refresh Anki browser after updating note
refresh_gui_after_update=yes

# --- Yomitan settings ---

yomitan_url=127.0.0.1:19633

# Field handlebars; leave empty to disable
# See https://github.com/BrenoAqua/Yomipv/blob/main/docs/field_handlebars.md for more info
selection_text_handlebar=popup-selection-text
definition_handlebar=selected-dict
glossary_handlebar=glossary

# Highlighting tag
sentence_highlight_tag=<b>

# HTML wrappers
primary_sentence_wrapper=<span class="group">%s</span>
secondary_sentence_wrapper=<span class="group">%s</span>
miscinfo_wrapper=<span class="group">%s</span>

# Preferred dictionary (Senren only)
dictionary_pref_value=
#dictionary_pref_value=Jitendex

# --- Media templates ---

audio_template=[sound:%s]
image_template=<img src="%s" class="yomipv-image">

# --- Picture settings ---

picture_use_ffmpeg=yes
# Capture timing: subtitle_start, current_position
picture_timestamp_source=subtitle_start
picture_animated=no

# Static screenshot settings
picture_static_format=avif
picture_static_quality=85
picture_static_width=1080
picture_static_offset=0.0

# Animated picture settings
animation_format=avif
animation_quality=50
animation_width=720
animation_fps=10
animation_duration=auto
animation_offset=0.0
animation_end_offset=0.0

# Advanced codec settings
picture_webp_lossless=no
picture_webp_compression=6
picture_avif_cpu_used=4

# --- Audio clip settings ---

audio_use_ffmpeg=yes
audio_format=opus
audio_bitrate=64k
audio_offset=0.0
audio_end_offset=0.0
filename_show_ms=yes
audio_match_volume=no

# --- Misc info settings ---

# Format specifiers:
# {name}: Sanitized title (removes episodes and brackets)
# {season}: Season info
# {episode}: Episode info
# {timestamp}: Timestamp (HH:MM:SS)

# Bullet logic (miscinfo_episode_bullet):
# Adapts dynamically to {season} and {episode} presence
# If both {season} and {episode}: Bullet matches {season}, comma separator added
# If only {episode}: Bullet matches {episode}

# Formatting examples:
# {name}{season}{episode} • {timestamp} -> Show Name • シーズン 2, エピソード 6 • 00:03:34
# {name}{episode} • {timestamp}         -> Show Name • エピソード 6 • 00:03:34

miscinfo_episode_bullet=yes
miscinfo_show_season_one=no
miscinfo_show_ms=no
miscinfo_episode_label=エピソード
miscinfo_season_label=シーズン
miscinfo_format={name}{season}{episode} • {timestamp}

# --- Note tagging settings ---
# {name}, {season}, {episode}, and {timestamp} can also be used with the note tag
note_tag=アニメ

# --- Selector settings ---

# Behavior
pre_tokenize=yes
selector_show_history=no
selector_hide_ui=yes
selector_navigation_delay=0.05
selector_trigger_on_mouse_move=no
selector_trigger_mouse_idle_time=5

# --- Colorizer settings ---
# Uses your local Anki word database to color subtitle words by card state
# See https://github.com/BrenoAqua/Yomipv/blob/main/docs/colorizer.md for more info

# Replace MPV subtitles with Yomipv-rendered subtitles
colorizer_enabled=no
# Main toggle for word coloring
selector_colorize_words=yes
# Skip kana-only words with no database match
colorizer_ignore_kana_only=no
# Show colors on underlines instead of words
selector_colorize_underline=yes
# Opacity of colorized underlines from 0 to 100
selector_colorize_opacity=100

# Lookup
# See https://github.com/BrenoAqua/Yomipv/blob/main/docs/lookup-app.md for more info
selector_lookup_on_hover=yes
selector_lookup_on_navigation=no
selector_lookup_delay=0.1
selector_mora_hover=yes
selector_mora_navigation=no
lookup_show_frequencies=yes
lookup_show_pitch_accents=yes
# Prioritize entries with Kanji over match length
prioritize_kanji_match=no
# Prioritize hiragana-only entries when term is hiragana-only
prioritize_hiragana_match=no

# Lookup theme: dark, light
lookup_theme=dark

# Typography
selector_font_name=Hiragino Kaku Gothic ProN W6
selector_font_size=45
selector_line_height=1.25

# Appearance
selector_selection_underline=no
selector_underline_thickness=4
selector_underline_offset=-1
selector_border_size=2
selector_shadow_offset=0

# Colors
selector_color=#FFFFFF
selector_selection_color=#56FF68
selector_lock_color=#FFD700
selector_persistent_color=#FF8C00
selector_border_color=#000000
selector_shadow_color=#000000

# Layout
selector_pos_y=100
selector_max_width_factor=0.9

# Keybindings
key_toggle_colorizer=S
key_open_selector=c
key_selector_confirm=ENTER,c
key_selector_cancel=ESC
key_selector_left=LEFT
key_selector_right=RIGHT
key_selector_up=UP
key_selector_down=DOWN
key_expand_prev=Shift+LEFT
key_expand_next=Shift+RIGHT
key_selector_lookup=Ctrl+c
key_selector_lock=v
key_toggle_mora_navigation=s
key_toggle_selector_trigger_on_mouse_move=z
key_append_mode=C
key_set_timing_start=q
key_set_timing_end=w
key_clear_timings=e
key_build_ankidb=B
key_toggle_picture_animated=g
key_toggle_picture_timestamp_source=Ctrl+g

# --- History settings ---

# Behavior
history_show_secondary=yes
history_hide_volume=yes
history_max_entries=200

# Size
history_width=380
history_max_height=60vh

# Typography
history_font_size=16
history_secondary_font_size=13
history_font_family=

# Appearance
history_accent_color=#3db54a
history_header_background_color=rgba(255, 255, 255, 0.03)
history_background_color=#111111
history_background_opacity=85%
history_border_radius=8

# Keybindings
key_toggle_history=a
key_history_clear=x

# --- Subtitle settings ---

# Behavior
subtitle_filter_enabled=yes

# Primary subtitles
primary_autoload=yes
# Preferred languages for primary subtitles
primary_sub_lang=ja,jpn
# Skip tracks with matching keywords
primary_sub_exclude=signs,songs,forced,commentary,sdh,cc
# Automatically sync primary subtitle timing to secondary if languages match
auto_sync_subtitles=no

# Secondary subtitles
secondary_sid=yes
secondary_on_hover=yes
# Preferred languages for secondary subtitles
secondary_sub_lang=en,eng
# Skip tracks with matching keywords
secondary_sub_exclude=signs,songs,forced,commentary,sdh,cc

# Keybindings
key_sub_seek_next=Alt+RIGHT
key_sub_seek_prev=Alt+LEFT
key_sub_replay=r
key_secondary_sub_next=Ctrl+j
key_secondary_sub_prev=Ctrl+J
key_sync_subtitles=Ctrl+s

# --- AniList settings ---

anilist_enabled=no
anilist_token=
anilist_update_thresh_percent=80
anilist_show_notifications=yes
key_anilist_auth=Ctrl+a

# --- Updater settings ---

updater_enabled=yes
updater_check_on_startup=yes
#updater_use_source=no
key_update=U

# --- MPV Settings ---

# Show OSD messages
osd_messages=yes

# --- Settings ---

# Automatically load Yomipv on startup
auto_load=yes

# Active profile
current_profile=default

# Keybindings
key_load_yomipv=y
key_open_settings=Ctrl+i
key_cycle_profile=Ctrl+p
